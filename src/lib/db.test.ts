import { describe, it, expect } from "vitest";

describe("DB connection", () => {
  it("should connect to the database", async () => {
    expect(true).toBe(true); // basit test, DB testini ekleyebilirsin
  });
});
